name: EAGLE-I 2022 diagnostic

on:
  push:
    branches:
      - external-severity-ablation
    paths:
      - "analysis/eaglei_2022_diagnostic.py"
      - ".github/workflows/eaglei-2022-diagnostic.yml"
  workflow_dispatch:

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          ref: external-severity-ablation

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy requests openpyxl

      - name: Run EAGLE-I diagnostic
        run: python analysis/eaglei_2022_diagnostic.py

      - name: Upload diagnostic artifact
        uses: actions/upload-artifact@v4
        with:
          name: eaglei-2022-diagnostic
          path: audit_outputs/eaglei_2022/
