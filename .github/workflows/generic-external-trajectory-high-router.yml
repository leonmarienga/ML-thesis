name: Generic external trajectory high router

on:
  push:
    branches:
      - experiment/mission-composition-20260829
    paths:
      - 'generic_external_trajectory_high_router_experiment.py'
      - '.github/workflows/generic-external-trajectory-high-router.yml'
  workflow_dispatch:

jobs:
  screen:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy openpyxl scikit-learn catboost requests scipy
      - name: Run cross-hazard trajectory experiment
        run: python generic_external_trajectory_high_router_experiment.py
      - name: Upload trajectory results
        uses: actions/upload-artifact@v4
        with:
          name: generic-external-trajectory-high-router-results
          path: generic_external_trajectory_high_router_results/
          retention-days: 30
