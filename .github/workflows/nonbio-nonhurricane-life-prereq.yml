name: Nonbio non-Hurricane life prerequisite audit

on:
  push:
    branches:
      - nonbiological-fire-verifier
    paths:
      - "analysis/nonbio_nonhurricane_life_prereq.py"
      - ".github/workflows/nonbio-nonhurricane-life-prereq.yml"
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          ref: nonbiological-fire-verifier
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy scipy scikit-learn requests openpyxl pyarrow duckdb
      - name: Run non-Hurricane life prerequisite audit
        run: python analysis/nonbio_nonhurricane_life_prereq.py
      - uses: actions/upload-artifact@v4
        with:
          name: nonbio-nonhurricane-life-prereq
          path: audit_outputs/nonbio_nonhurricane_life_prereq/
