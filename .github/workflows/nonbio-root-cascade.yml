name: Nonbio two-stage root cascade

on:
  push:
    branches:
      - nonbiological-fire-verifier
    paths:
      - "analysis/nonbio_root_cascade.py"
      - ".github/workflows/nonbio-root-cascade.yml"
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          ref: nonbiological-fire-verifier
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy scipy scikit-learn requests openpyxl pyarrow duckdb
      - name: Run two-stage root cascade audit
        run: python analysis/nonbio_root_cascade.py
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: nonbio-root-cascade
          path: audit_outputs/nonbio_root_cascade/
